Configuration:
  status: debug
  name: Default
  
  appenders:
    Console:
      name: stdout
      PatternLayout:
        pattern: "[%-5level] %d{yyyy-MM-dd HH:mm:ss.SSS} [%t] %c{1} - %msg%n"
        
  Loggers:
    Root:
      level: debug
      AppenderRef:
        ref: stdout
